name: Release
on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.os }} / ${{ matrix.goos }}-${{ matrix.goarch }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            goos: linux
            goarch: amd64
            binary_ext: ""
          - os: ubuntu-latest
            goos: linux
            goarch: arm64
            binary_ext: ""
          - os: macos-latest
            goos: darwin
            goarch: amd64
            binary_ext: ""
          - os: macos-latest
            goos: darwin
            goarch: arm64
            binary_ext: ""

    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-go@v5
        with:
          go-version: stable

      - name: Build
        run: |
          mkdir -p dist
          GOOS=${{ matrix.goos }} GOARCH=${{ matrix.goarch }} CGO_ENABLED=0 \
            go build -trimpath -ldflags="-s -w" \
            -o dist/optml-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.binary_ext }} .
        shell: bash

      - name: Upload artifact
        uses: actions/upload-artifact@v7
        with:
          name: optml-${{ matrix.goos }}-${{ matrix.goarch }}
          path: dist/optml-${{ matrix.goos }}-${{ matrix.goarch }}${{ matrix.binary_ext }}

  release:
    name: Create GitHub Release
    needs: build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6

      - name: Download all artifacts
        uses: actions/download-artifact@v8
        with:
          path: dist

      - name: Generate checksums
        run: |
          cd dist
          sha256sum **/* > SHA256SUMS.txt

      - name: Read changelog section for tag
        id: release_notes
        run: |
          RELEASE_NOTES="$(./scripts/changelog_for_tag.bash "${{ github.ref_name }}")"
          {
            echo "body<<__RELEASE_NOTES__"
            printf '%s\n' "$RELEASE_NOTES"
            echo "__RELEASE_NOTES__"
          } >> "$GITHUB_OUTPUT"

      - name: Create release
        uses: softprops/action-gh-release@v3
        with:
          tag_name: ${{ github.ref_name }}
          body: ${{ steps.release_notes.outputs.body }}
          files: dist/**/*
